[project]
name = "nar"
version = "0.1.0"
description = "Autonomous network research via ns-3"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10",
    "numpy>=2.2",
    "pandas>=2.3",
]
